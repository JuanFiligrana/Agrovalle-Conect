# Capa de presentación
Spring Boot sirve la vista estática desde `src/main/resources/static/index.html`.
El directorio views documenta esa ubicación y evita duplicar el HTML. Los modelos
están en el paquete models y los controladores HTTP en controllers.
